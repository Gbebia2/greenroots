<section class="products">
  <h2>Our Products</h2>
  <div class="product-container">
    <% products.forEach(product => { %>
      <div class="product-card">
        <img src="<%= product.image %>" alt="<%= product.name %>">
        <h3><%= product.name %></h3>
        <a href="/products/<%= product.id %>" class="btn-order">View Details</a>
      </div>
    <% }) %>
  </div>
</section>